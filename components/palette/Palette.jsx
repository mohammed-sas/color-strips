import { Box } from "@chakra-ui/react"


const Palette = ({palette}) => {
    return (
        <Box h="2rem" bgColor={palette} flexGrow={1}>
            
        </Box>
    )
}

export default Palette
